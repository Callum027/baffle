service http tcp port 80
service https tcp port 443
group service www { http, https }

entity h4 ipv4 192.0.2.1/24

entity h5 ipv4 192.0.2.2/24
entity h5 service group www

entity h6 ipv4 192.0.2.3/24

# TODO: entity for subnet? some way to abstract from the policy itself
# TODO: add entity groups to "app" in policy

policy h4_no_h5_www deny entity h4 entity h5 group www 
policy h5_www allow ipv4_src 192.0.2.0/24 entity h5 group www
